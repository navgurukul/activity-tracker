version: 1
frontend:
  phases:
    preBuild:
      commands:
        - npm install -g pnpm@10.30.3
        - pnpm install
    build:
      commands:
        - 'env | grep -E "NEXT_PUBLIC_|BACKEND_" >> .env.production || true'
        - pnpm run build
  artifacts:
    baseDirectory: .next
    files:
      - "**/*"
  cache:
    paths:
      - node_modules/**/*
      - .next/cache/**/*
